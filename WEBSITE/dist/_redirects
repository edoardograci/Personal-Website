# Serve robots.txt directly
/robots.txt    /robots.txt    200

# Serve sitemap.xml directly
/sitemap.xml    /sitemap.xml    200

# Handle the root path
/    /.netlify/functions/framer-proxy    200!

# Handle all other paths with splat
/*    /.netlify/functions/framer-proxy/:splat    200!

# Handle assets directly
/assets/*    https://charismatic-everyone-653587.framer.app/assets/:splat    200!
